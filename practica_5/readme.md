## Equipo

**Educación**

## Leyes

1. Existe una sola respuesta correcta para cada pregunta.
2. Si la respuesta no coincide con la correcta → es incorrecta.
3. Si el usuario envía una respuesta incorrecta → se le devuelve el enlace de apoyo.
4. Si la respuesta no coincide con la correcta → es incorrecta.

## Hechos

### Diseño de Algoritmos

(respuesta(usuario, algoritmo) = "Un error de sintaxis"
∨ respuesta(usuario, algoritmo) = "Un lenguaje de programación"
∨ respuesta(usuario, algoritmo) = "Una estructura de datos"
∨ respuesta(usuario, representacion) = "Tablas de verdad"
∨ respuesta(usuario, representacion) = "Código binario"
∨ respuesta(usuario, representacion) = "Estructuras de control"
∨ respuesta(usuario, primer_paso) = "Codificarlo en C"
∨ respuesta(usuario, primer_paso) = "Probarlo directamente"
∨ respuesta(usuario, primer_paso) = "Hacer un diagrama UML"
∨ respuesta(usuario, funcion) = "Una variable que almacena datos"
∨ respuesta(usuario, funcion) = "Un error en tiempo de ejecución"
∨ respuesta(usuario, funcion) = "Una estructura de bucle")
→ necesita_reforzar(usuario, "Diseño de Algoritmos")

shell
Copiar código

### Introducción a la Programación

(respuesta(usuario, programar) = "Jugar videojuegos"
∨ respuesta(usuario, programar) = "Diseñar hardware"
∨ respuesta(usuario, programar) = "Traducir textos"
∨ respuesta(usuario, caracteristica) = "Son ambiguos"
∨ respuesta(usuario, caracteristica) = "No requieren reglas"
∨ respuesta(usuario, caracteristica) = "Sólo funcionan en una computadora específica"
∨ respuesta(usuario, estructura) = "Inclusión de librerías"
∨ respuesta(usuario, estructura) = "Declaración de funciones"
∨ respuesta(usuario, estructura) = "Ejecución del main()"
∨ respuesta(usuario, literal) = "x"
∨ respuesta(usuario, literal) = "int"
∨ respuesta(usuario, literal) = "scanf"
∨ respuesta(usuario, compilador) = "Diseña interfaces gráficas"
∨ respuesta(usuario, compilador) = "Ejecuta directamente el programa"
∨ respuesta(usuario, compilador) = "Revisa el hardware del sistema")
→ necesita_reforzar(usuario, "Introducción a la Programación")

shell
Copiar código

### Control de Flujo

(respuesta(usuario, secuencial) = "La repetición de bloques"
∨ respuesta(usuario, secuencial) = "La toma de decisiones condicional"
∨ respuesta(usuario, secuencial) = "La recursión"
∨ respuesta(usuario, selectiva) = "for"
∨ respuesta(usuario, selectiva) = "while"
∨ respuesta(usuario, selectiva) = "do-while"
∨ respuesta(usuario, iterativa) = "if"
∨ respuesta(usuario, iterativa) = "switch"
∨ respuesta(usuario, iterativa) = "return")
→ necesita_reforzar(usuario, "Control de Flujo")

shell
Copiar código

### Organización de Datos

(respuesta(usuario, arreglo) = "Una variable que almacena un solo valor"
∨ respuesta(usuario, arreglo) = "Una función matemática"
∨ respuesta(usuario, arreglo) = "Un bucle infinito"
∨ respuesta(usuario, unidimensional) = "Tiene más de una fila y columna"
∨ respuesta(usuario, unidimensional) = "Es una función"
∨ respuesta(usuario, unidimensional) = "Solo acepta caracteres"
∨ respuesta(usuario, bidimensional) = "Almacenar un solo número"
∨ respuesta(usuario, bidimensional) = "Definir variables"
∨ respuesta(usuario, bidimensional) = "Controlar el flujo"
∨ respuesta(usuario, struct) = "Repetir instrucciones"
∨ respuesta(usuario, struct) = "Traducir código"
∨ respuesta(usuario, struct) = "Ejecutar bucles")
→ necesita_reforzar(usuario, "Organización de Datos")

shell
Copiar código

### Modularidad

(respuesta(usuario, modulo) = "Un archivo de configuración del sistema"
∨ respuesta(usuario, modulo) = "Un ciclo infinito"
∨ respuesta(usuario, modulo) = "Una variable global"
∨ respuesta(usuario, parametros) = "Evitar errores de sintaxis"
∨ respuesta(usuario, parametros) = "Hacer que el programa se detenga"
∨ respuesta(usuario, parametros) = "Evitar el uso de funciones"
∨ respuesta(usuario, implementacion) = "Traducirlo a otro lenguaje"
∨ respuesta(usuario, implementacion) = "Borrarlo del sistema"
∨ respuesta(usuario, implementacion) = "Ejecutarlo sin definirlo")
→ necesita_reforzar(usuario, "Modularidad")
